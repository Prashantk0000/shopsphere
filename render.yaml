services:
  - type: web
    name: shopsphere
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn shopsphere_project.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: DATABASE_URL
        fromDatabase:
          name: shopsphere-db
          property: connectionString

databases:
  - name: shopsphere-db
    plan: free
